.env.local
node_modules/
.next/
.DS_Store
*.log

.vercel
.env*.local

# Build artifacts and debug scratch output.
# These are regenerated by `tsc`/`next build` on every run, so tracking them
# means a guaranteed conflict whenever two branches have both been built.
*.tsbuildinfo
build-log.txt
build-output.txt
tsc-errors*.txt
tsc-test*.txt
.env*
